services:
  unifi-proxy:
    image: spinogrizz/unifi-proxy:latest
    ports:
      - "${HTTP_PORT:-80}:80"
      - "${HTTPS_PORT:-443}:443"
      - "${MQTT_PORT:-8883}:8883"
    # volumes:
    #   - ./guide:/var/www/guide:ro
    restart: unless-stopped
