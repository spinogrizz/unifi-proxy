# Автоматическое обновление

Основной сценарий использования — прописать в DNS настройках мой сервер вместо адресов серверов обновлений. Это позволит всем UniFi консолям работать с ними прозрачно — обновления начнут приходить автоматически и скачиваться так, как будто оборудование находится не в России.

Наличие VPN при этом не нужно, весь фокус работает через подмену DNS адресов серверов обновлений, и из вашей сети есть доступ до моего сервера (IP адрес 103.167.234.147), то обновления будут работать.

---

### 🌐  Проксируемые домены

Для работы этой схемы, нужно прописать следующие домены в DNS настройках UniFi и перенаправить их на мой сервер. Перенаправить можно как через CNAME (синононим), так и через A-запись напрямую на IP адрес.

Корректно работать CNAME начал только в UniFi Network 10.0+, поэтому если у вас версия ниже, то нужно использовать A-запись.

Используются следующие домены: `fw-download.ubnt.com`, `fw-update.ubnt.com`, `fw-update.ui.com`, `fw-download.ui.com`, `apt.artifacts.ui.com`.

Если вы используете Release Candidate или Beta прошивки, то нужно прописать дополнительные домены: `apt-release-candidate.artifacts.ui.com`, `apt-beta.artifacts.ui.com`.

Было замечено, что не всегда DNS-настройки в UniFi Network интерфейсе не всегда применяются по отношению к самому роутеру (действуют только на клиентов), поэтому самый «железный» способ — прописать вручную в `/etc/hosts` на роутере, но он требует больше работы и может быть перезаписан при обновлении прошивки.


### 🤖  Настройка через CNAME


<img src="example-cname.png" align="left" style="margin-right: 32px; margin-top: 16px; width: 270px; height: auto;" />

Самый простой способ — создать CNAME записи в DNS UniFi. CNAME это инструкция для DNS-сервиса, которая говорит о том, что для получения реального IP-адреса нужно сначала получить IP адрес unifi.gryzlov.com, а затем использовать его для доступа к нужному домену.

Идите *Settings* → *Policy Table* и создавайте новый Policy типа DNS. Выбираете для каждой записи тип «Alias (CNAME)» и задаете в первом поле исходный домен обновлений, а во втором **unifi.gryzlov.com**.

Прописывать домен удобнее, так как IP-адрес может (вдруг) поменяться и вы точно не забудете, куда именно проксируете.

<br clear="left" />
<br/>

| Alias Domain Name | Target Domain Name |
|----------|-------------|
| `fw-download.ubnt.com` | `unifi.gryzlov.com` |
| `fw-update.ubnt.com` | `unifi.gryzlov.com` |
| `fw-update.ui.com` | `unifi.gryzlov.com` |
| `fw-download.ui.com` | `unifi.gryzlov.com` |
| `apt.artifacts.ui.com` | `unifi.gryzlov.com` |
| `apt-release-candidate.artifacts.ui.com` | `unifi.gryzlov.com` |
| `apt-beta.artifacts.ui.com` | `unifi.gryzlov.com` |



---


### ⚒️  Настройка через A-запись

Если ваш Network (9.0 или ранее) не поддерживает CNAME, то можно использовать A-запись. Подобная запись напрямую ссылается на IP адрес, минуя синонимы (CNAME).

Список доменов точно такой же, только вместо **unifi.gryzlov.com** нужно прописать IP адрес `103.167.234.147`.


---

### 💪  Вручную через /etc/hosts

На некоторых прошивках, сам роутер/консоль может игнорировать все Policy создаваемые в интерфейсе. Они будут работать только для клиентов, а сам роутер продолжит ходить на дефолтные DNS сервера.

Чтобы этого избежать, нужно прописать адреса вручную в **/etc/hosts** на роутере.

1. Сначала включите SSH в настройках UniFi. В разных версиях UniFi Network это может выглядеть по-разному, поэтому используйте поиск по настройкам. Вам нужно включить SSH и задать пароль для подключения.

2. Подключитесь к роутеру по SSH (используя терминал в macOS/Linux или Putty в Windows). Пользователь root, IP адрес роутера.
 
3. Войдите в редактор **/etc/hosts**:
   ```bash
   vi /etc/hosts
   ```

4. Если вы ранее не пользовались vi, то нужно нажать **i** для перехода в режим редактирования, потом вставить строчки через буфер обмена, а потом нажать **Esc** для выхода из режима редактирования и набрать на клавиатуре три символа (строго) — **:wq**. Это команда сохранит файл и выйдет из редактора. Если ничего не вводится, значит вы не вошли в режим редактирования через **i**. 

Вам нужно вставить следующие строчки в самый конец файла. Важно: не сотрите ничего в начале файла и не повредите, вставлять нужно строго в конец файла.


```
103.167.234.147  fw-download.ubnt.com
103.167.234.147  fw-update.ubnt.com
103.167.234.147  fw-update.ui.com
103.167.234.147  fw-download.ui.com
103.167.234.147  apt.artifacts.ui.com
103.167.234.147  apt-release-candidate.artifacts.ui.com
103.167.234.147  apt-beta.artifacts.ui.com
```

5. Для проверки того, что все добавлено корректно, можете использовать команду **cat /etc/hosts** или сделать тут же проверку DNS как описано ниже.

---

### ✅  Как проверить

Если вы имеете SSH-доступ к роутеру, то после прописывания DNS-записей можно сделать проверку DNS резолва прямо в терминале.

Эту же команду можно запустить на любой машине в локальной сети, команда универсальна для macOS/Linux/Windows.

```bash
nslookup fw-download.ubnt.com
```

В результате выполнения этой команды должен вернуться IP **103.167.234.147** и/или перенаправление на **unifi.gryzlov.com**.

Или можно тут же попробовать скачать какой-нибудь файл с сайта Ubiquiti, чтобы убедиться, что все работает:

```bash
wget https://dl.ui.com/unifi/firmware/U7PRO/8.3.2.18064/BZ.ipq53xx_8.3.2+18064.251210.1649.bin
```


<style>

table {
    width: 100%;
    max-width: 100%;
    overflow-x: auto;
}

</style>